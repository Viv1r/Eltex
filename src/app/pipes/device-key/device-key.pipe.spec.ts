import { DeviceKeyPipe } from './device-key.pipe';

describe('DeviceKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
